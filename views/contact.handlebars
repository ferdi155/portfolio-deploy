<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
</head>
<body>
    <div class="w-full ">
        <h1 class="text-4xl mb-2 font-bold text-center">Kontak</h1>
        <p class="font-base/loose text-center mb-10 opacity-50">
          Mari terhubung dengan saya
        </p>
        <form
          action="https://formsubmit.co/alfnsh155@gmail.com"
          method="POST"
          class="bg-gray-200 p-10 sm:w-fit w-full mx-auto rounded-md"
          autoComplete="off"
        >
          <div class="flex flex-col gap-6">
            <div class="flex flex-col gap-2">
              <label class="font-semibold">Nama Lengkap</label>
              <input
                type="text" name="nama" placeholder="Masukan Nama...." class="bg-white shadow-xl p-2 rounded-md"
                required
              />
            </div>
            <div class="flex flex-col gap-2">
              <label class="font-semibold">Email</label>
              <input type="email" name="email" placeholder="Masukan Email...." class="bg-white shadow-xl  p-2 rounded-md" required/>
            </div>
            <div class="flex flex-col gap-2">
              <label htmlFor="pesan" class="font-semibold">
                Pesan
              </label>
              <textarea name="pesan" id="pesan" cols="45" rows="5" placeholder="Pesan...." class="bg-white shadow-xl  p-2 rounded-md"></textarea>
            </div>
            <div class="tect-center">
              <button type="submit" class="bg-gray-900 p-3 text-white rounded-lg shadow-md w-full cursor-pointer hover:bg-gray-700">
                Kirim Pesan
              </button>
            </div>
          </div>
        </form>
      </div>

</body>
</html>